#include <stdio.h>
#include "selecao.h"

int linear_search(int *array, size_t size, int target) {
    printf("Hello, World!\n");
    return 0;
}

int quick_select(int *array, size_t size, int k) {
    printf("Hello, World!\n");
    return 0;
}